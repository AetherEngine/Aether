//! Aliases for upstream constants. Keep foreign names in this binding file.

const vk = @import("vulkan");

pub const api_version_1_4 = vk.API_VERSION_1_4;
pub const queue_family_ignored = vk.QUEUE_FAMILY_IGNORED;
pub const whole_size = vk.WHOLE_SIZE;
